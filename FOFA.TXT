<!doctype html>
<html lang="zh-CN">
  <head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1" />
    <title>è¯·æ±éçè¿å¿«</title>
    <style>
      :root {
        --main-color: #05f2f2;
        --level1-bg-color: #040c1f;
        --primary-font-color: #ffffff;
      }

      * {
        box-sizing: border-box;
      }

      html,
      body {
        min-height: 100%;
      }

      body {
        margin: 0;
        color: var(--primary-font-color);
        font-family: -apple-system, BlinkMacSystemFont, "Segoe UI", Arial, sans-serif;
        background: var(--level1-bg-color);
      }

      .content-container {
        min-height: 100vh;
        height: 100vh;
        padding: 24px;
        display: flex;
        align-items: center;
        justify-content: center;
      }

      .error-page {
        width: min(760px, 100%);
        text-align: center;
        transform: translateY(-4vh);
      }

      .notice-image {
        display: block;
        width: 360px;
        max-width: min(360px, 76vw);
        height: auto;
        margin: 0 auto 28px;
      }

      .message {
        margin: 10px 0;
        color: var(--primary-font-color);
        font-size: 18px;
        font-weight: 600;
        line-height: 1.7;
      }

      @media (max-width: 520px) {
        .content-container {
          padding: 16px;
        }

        .error-page {
          transform: translateY(-2vh);
        }

        .notice-image {
          width: 300px;
          max-width: 82vw;
          margin-bottom: 24px;
        }

        .message {
          font-size: 16px;
        }
      }
    </style>
  </head>
  <body>
    <main class="content-container">
      <section class="error-page" role="alert" aria-labelledby="rate-limit-message">
        <img class="notice-image" src="/public/assets/images/img-notice.png" alt="" aria-hidden="true" />
        <p id="rate-limit-message" class="message">è¯·æ±éçè¿å¿«ï¼è¯·å·æ°é¡µé¢æéè¯ãï¼3ç§å°èªå¨å·æ°ï¼</p>
      </section>
    </main>
    <script>
      (function () {
        var seconds = 3;
        var params = new URLSearchParams(window.location.search);
        var isEnglish =
          params.get('lang') === 'en' ||
          window.location.hostname === 'en.fofa.info' ||
          window.location.hostname.endsWith('.en.fofa.info');

        var messageEl = document.getElementById('rate-limit-message');

        function render() {
          if (isEnglish) {
            document.documentElement.lang = 'en';
            document.title = 'Too Many Requests';
            messageEl.textContent =
              'Too many requests, please refresh and try again. (Will refresh in ' +
              seconds +
              ' sec)';
            return;
          }

          document.documentElement.lang = 'zh-CN';
          document.title = 'è¯·æ±éçè¿å¿«';
          messageEl.textContent = 'è¯·æ±éçè¿å¿«ï¼è¯·å·æ°é¡µé¢æéè¯ãï¼' + seconds + 'ç§å°èªå¨å·æ°ï¼';
        }

        render();

        var timer = window.setInterval(function () {
          seconds -= 1;

          if (seconds <= 0) {
            window.clearInterval(timer);
            window.location.reload();
            return;
          }

          render();
        }, 1000);
      })();
    </script>
  </body>
</html>
